// Firebase removed — stubs kept so imports compile without changes
export function getFirebaseAdmin(): boolean { return false; }
export async function verifyFirebaseToken(_idToken: string): Promise<null> { return null; }

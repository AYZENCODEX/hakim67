import EntityDashboardTabs from "@/components/entity-dashboard-tabs";

// Vault "Entity" tab -> Dashboard view.
// Aggregate dashboard across every entity in the vault: Overview / Project / PnL tabs.
export default function VaultEntityDashboard() {
  return <EntityDashboardTabs />;
}
